https://github.com/cyberspacedk/Git-commands
//добавить в локальный залить на удалённый
cd /f/Spring/My/layer-cake
git init
git add .
git commit -m "This is a new commit for what I originally planned to be amended"
git remote add origin https://github.com/umdomby/....
git branch -M main
git push origin main

//обновляем
git remote add origin https://github.com/umdomby/test2.git
git branch -M main
git push -u origin main


git checkout master           # переключаемся на ветку master
git checkout -b new # создадим новую ветку 
git branch -d new # удаляем ветку new_page_header


git pull origin master
Забираем изменения из ветки (master) на удаленном сервере (origin) и проводим слияние с активной веткой.

git config --global user.name “Navatar”
git config --global.email “umdom2@gmail.com”
cd /F/Spring/My/sweater-JPA_Postgres_lessonsпше
git status – текущее состояние

//заменить локальный репозиториый
git status
git add .    (git pull попробывать)
git commit -m "2"

git remote origin https://github.com/umdomby/UMDOM1Server.git
git push

Чтобы объединить ветки, например, ветку hotfix с веткой master, необходимо выполнить следующие команды:
$ git checkout master
$ git merge hotfix

показывает имя удаленного репо, если такой имеется в наличии.
$ git remote –v



----------------------SDK----------------------
$ curl -s "https://get.sdkman.io" | bash
Переключиться между версиями «u»


ssh -i /F/Amazon/119.pem ec2-user@ec2-3-128-153-137.us-east-2.compute.amazonaws.com

java -cp springbootdemo.jar net.proselyte.springbootdemo.SpringbootdemoApplication


cd /F/GitClone/layercake/out/artifacts/layercake_jar

cd /F/GitClone/layercake/out/artifacts/layercake_jar



cd /F/Amazon/springbootdemo-master/out/artifacts/springbootdemo_jar


cd /F/Amazon/springb/out/artifacts/springb_jar


java -jar springbootdemo.jar


https://www.youtube.com/playlist?list=PLU2ftbIeotGpAYRP9Iv2KLIwK36-o_qYk
